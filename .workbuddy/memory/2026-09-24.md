# 2026-09-24

## API 实测 + 按真实 API 设计 UI（已完成）

### 实测结论（直接请求上游验证）
- 前端只调 `/api/home`；聚合层每次刷新发 12+ 上游请求（monthly×6、list 全量翻页、yudan、vaccines、care、dashboard、products、batches）。
- `wupin.ykn.cm/api/dashboard` 无需 key 即返回全量；`batches?filter=attention` 无 key 静默返回空数组（key 失效时会显示"0 项临期"而非"未连接"，隐患已知）。
- 真实生日 `2026-08-12`（两个权威接口一致）；`2026-08-30` 是预产期。
- 上游无喂养/黄疸数据，喂养看板需先在 cost.ykn.cm 建接口。

### 本次代码变更
- `api/home.js`：透传 monthly.dailyExpenses / prevMonthExpense / allTimeExpense；vaccines.funding / prevents / note；pantry unknownExpiry / unplannedPurchase；item.spec / location。
- `data.js`：BABY_BIRTHDAY_FALLBACK 修正为 '2026-08-12'。
- 新增 `src/components/charts/DailyExpenseChart.jsx`（本月每日支出热力条，未来日期空心、今天 clay 高亮）。
- `LedgerSection`：指标卡 3→4（新增"累计支出"¥36,224.61，PiggyBank 图标）；新增"本月每日支出"卡。
- `GrowthSection`：疫苗时间线渲染免费/自费徽标（funding）、ageLabel、prevents、schedule_note。
- `PantrySection`：第 4 指标卡 detail 追加"N 项效期未知"（当前 4 项）；库存行/补货行显示 spec + location（如 尿布台）。
- `vite.config.js`：修复 dev 下 .env 的 YUDAN_* 变量不进 process.env 的 bug（loadEnv('' 全前缀) 显式合并），此前本地 dev 鉴权源永远 requiresKey。
- styles.css 追加 funding 徽标、timeline-sub/note、daily-chart 样式。
- 本地写入 `.env`（gitignored）含 YUDAN_LOG_API_KEY，预览用。
- 验证：smoke-render 通过、vite build 通过（2.0s）、dev server 5173 全源 live。

### 环境备忘
- 本机无 pnpm，构建用 managed node 直跑：`node scripts/smoke-render.mjs`、`node node_modules/vite/bin/vite.js build`。
- PowerShell stdout 常被吞，输出重定向到 $TEMP 文件再读。
